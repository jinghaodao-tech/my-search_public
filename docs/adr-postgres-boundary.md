# PostgreSQL Boundary

PostgreSQL is an optional repository backend for shared or high-volume deployments. SQLite remains the default local-first store. The PostgreSQL adapter uses parameterized queries and transactions, while search quality remains validated against the same dataset and thresholds.
